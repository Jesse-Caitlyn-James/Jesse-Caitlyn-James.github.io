class ShopManager{
    constructor(){
        this.shopPanel = new Sprite(windowWidth * 0.78, windowHeight * 0.5, windowWidth * 0.37, windowHeight * 0.8);
        this.shopPanel.collider = "none";
        this.shopPanel.visible = false;
    }

    shopUpdate(){
        // Dudes, i gotta be real, i cannot get sprite layers to work for the life of me
        // Pls do this for me
    }
}